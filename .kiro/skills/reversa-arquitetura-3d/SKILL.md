---
name: reversa-arquitetura-3d
description: Creates interactive 3D visualizations of software architecture with Three.js, generating standalone camera-browsable HTML from JSON of modules and dependencies.
disable-model-invocation: true
license: MIT
compatibility: Claude Code, Codex, Cursor, Gemini CLI and other agents compatible with Agent Skills.
metadata:
  author: sandeco
  version: "1.0.0"
  framework: reversa
  team: shared-skills
  role: 3d-renderer
---

# Arquitetura 3D

Creates 3D visualizations of **software architecture** using Three.js. Always generates **standalone HTML** (single file, self-contained) with interactive 3D scene, camera controls (mouse, touch, keyboard), parameter sidebar and button to export the viewport as PNG.

The skill covers five visual modes established in software visualization, each with a dedicated reference in `references/`:

| Mode | When to use | Reference |
|------|-------------|------------|
| **Code City** | size/complexity overview of each file, default "code city" | `references/CODE_CITY.md` |
| **Dependency Graph 3D** | Dependency graph with repulsive force, 3D nodes | `references/DEPENDENCY_GRAPH_3D.md` |
| **Layer Stack** | Architectural layers (UI/Domain/Infra) stacked with flow arrows | `references/LAYER_STACK.md` |
| **Call Graph 3D** | Deeply explorable call tree | `references/CALL_GRAPH_3D.md` |
| **Architecture Tour** | Animated camera panning through scene with narrative overlay | `references/ARCH_TOUR.md` |

Three.js shared standards, lighting, controls, and performance live in `references/THREE_PATTERNS.md`. Error scenarios and handling in `references/ERRORS.md`.

## Fluxo de Trabalho

### 1. Receive the data

Data can come from:

- **Inline JSON**: user provides `modules.json` (list of modules) e/ou `deps.json` (dependency graph).
- **File path**: user points to JSONs in `reversa/docs/assets/data/` (generated by agent `/reversa-documentation`).
- **Requested to the user**: if the skill is invoked without data, ask for path or request inline pasting.

**Schema esperado de `modules.json`**:

```json
[
  {
    "name": "src/auth/login.ts",
    "folder": "src/auth",
    "loc": 142,
    "complexity": 8,
    "type": "code"
  }
]
```

**Schema esperado de `deps.json`** (orientado):

```json
{
  "nodes": [{ "id": "src/auth/login.ts" }, { "id": "src/auth/jwt.ts" }],
  "edges": [{ "from": "src/auth/login.ts", "to": "src/auth/jwt.ts", "weight": 1 }]
}
```

### 2. Choose the mode

If the user specified the mode, use that. If not, **suggest 2 or 3 options** based on the data:

| Tipo de dado | Modos recomendados |
|--------------|--------------------|
| `modules.json` with LOC + complexity | Code City (default), Layer Stack if clear pastas/camadas |
| `deps.json` with many edges | Dependency Graph 3D, Code City coloring hot path |
| Execution trace or call graph | 3D Call Graph |
| Explicit request for presentation | Architecture Tour combining two of the previous ones |

When the number of nodes exceeds **500**, apply **folder grouping** automatically and notify the user (record the decision in the footer of the generated HTML).

### 3. Generate the code

Consult `references/THREE_PATTERNS.md` for base setup (renderer, scene, camera, lighting, OrbitControls). Consult the specific reference of the chosen mode for the layout algorithm and materials.

**Fundamental rules**:

1. **Standalone HTML**: single file `.html` with everything embedded (CSS, JS, inline data in `<script id="data">`). When run by the Reversa Docs Team, the data comes from `window.RV_DATA.modules` and `window.RV_DATA.deps` (loaded by the `assets/js/data.js` that Publisher generates) and the `<script id="data">` is empty or missing. End pages **never** do `fetch()` for local files (breaks via `file://`).
2. **Local Three.js**: use `<script src="assets/vendor/three.min.js"></script>` pointing to the file downloaded by Publisher (version pinned to `agents/reversa-docs-publisher/references/vendor-pins.yaml`, today `three@0.147.0` IIFE). In isolated invocation mode outside the Docs team, accept CDN as fallback (`https://unpkg.com/three@0.147.0/build/three.min.js`), but **never** mix versions.
3. **Local OrbitControls**: use `<script src="assets/vendor/OrbitControls.js"></script>` (also IIFE, compatible with `three@0.147`). Do not use `examples/jsm/...` until the skill migrates to importmap + ESM.
4. **Renderer**: WebGLRenderer with antialiasing, device pixelRatio.
5. **Lighting**: HemisphereLight + DirectionalLight with soft shadows. For Code City, extra AmbientLight to fill.
6. **Camera**: PerspectiveCamera, initial position looking at the center of the scene from above and slightly angled. Distance derived from scene size.
7. **Performance**: use `InstancedMesh` when there are more than 200 elements of the same type. Maximum limit of 5,000 buildings in Code City without clustering.
8. **Responsiveness**: resize handler resizes renderer and adjusts camera aspect ratio.
9. **Sidebar**: right side, controls sliders/checkboxes/buttons in vertical layout. Each control has stable ID for `localStorage`.
10. **Export PNG**: button captures the canvas via `renderer.domElement.toBlob()`.

### 4. Structure of the generated HTML

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Arquitetura 3D | <!-- PROJECT_NAME --></title>
    <script src="assets/vendor/three.min.js"></script>
    <script src="assets/vendor/OrbitControls.js"></script>
    <script src="assets/js/data.js"></script>
    <style>
        body { margin: 0; overflow: hidden; font-family: system-ui, sans-serif; }
        #scene { position: fixed; inset: 0; }
        #sidebar { position: fixed; top: 0; right: 0; width: 280px; height: 100vh; padding: 16px; background: rgba(15,15,20,0.85); color: #eaeaea; overflow-y: auto; }
        #loader { position: fixed; inset: 0; display: flex; align-items: center; justify-content: center; font-size: 18px; background: #0a0a10; color: #eaeaea; }
/* Style Reversa: variants derive from the <body> data-style via the agent's external CSS */
    </style>
</head>
<body data-style="exploratory">
    <div id="loader">Carregando cena 3D...</div>
    <canvas id="scene"></canvas>
    <aside id="sidebar">
        <h3>Controles</h3>
        <!-- SIDEBAR_CONTROLS -->
        <button id="reset">Reset</button>
        <button id="export-png">Exportar PNG</button>
    </aside>
    <script id="data" type="application/json"><!-- DATA_JSON --></script>
    <script>
// OrbitControls IIFE exposes THREE.OrbitControls globally.
// 1. Load data from window.RV_DATA when running in the Docs team,
        //    or from <script id="data"> in standalone mode.
// 2. Configure scene, camera, renderer, lighting
        // 3. Build geometry according to the mode (Code City, Dep Graph, etc.)
// 4. Connect sidebar to scene parameters
// 5. Rendering loop and event handling
    </script>
</body>
</html>
```

### 5. Salvar e entregar

The output is always standalone HTML. Save to the path indicated by the orchestrator agent (usually `reversa/docs/arquitetura.html`).

When invoked outside the context of `/reversa-documentation`, ask for the destination path or use `<mode>-<timestamp>.html` in the current directory.

## Quality guidelines

- **Intuitive camera**: initial position shows the entire scene; OrbitControls with damping for smooth movement.
- **Cohesive materials**: limited palette (5 to 8 colors maximum); loaded colors indicate attribute (e.g. red for hot path, blue for lightweight modules).
- **Readable Labels**: use CSS2DRenderer or sprites; labels only appear on hover or zoom above a threshold to avoid polluting.
- **Visible loader**: scene starts with "Loading 3D scene..." overlay that disappears when the `requestAnimationFrame` of the first frame ends.
- **Graceful fallback**: if Three.js does not load (no internet), show message "This view requires loading the Three.js library. Connect to the internet and reload."
- **Basic accessibility**: keyboard navigation on the sidebar buttons; visible focus.
- **Language**: comments and texts visible in PT-BR. No dash.

## Code Guidelines

- **Modularity**: separate scene creation, geometry construction and interaction management into functions with clear names.
- **No dependencies other than Three.js and OrbitControls (located in `assets/vendor/`)**: do not import GSAP, dat.GUI, or any other lib without clear need.
- **Constants named at the top**: colors, sizes, thresholds in a visible configuration block.
- **Dispose**: when switching modes or regenerating, call `geometry.dispose()` and `material.dispose()` to avoid leakage.
- **Performance check**: before rendering, count nodes; if > 5,000 without instanced mesh, abort and show warning.

## Tratamento de erros

See `references/ERRORS.md` for common scenarios (inaccessible CDN, malformed JSON, empty project, unsupported WebGL, etc).
